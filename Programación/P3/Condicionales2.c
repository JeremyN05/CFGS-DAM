#include <stdio.h>
#include <stdlib.h>

/* OPERADORES DE COMPARACIÓN SON:
 * >mayor
 * <menor
 * >= Mayor O Igual
 * <= Menor O Igual
 * == igual
 * != Distinto
*/
int main(){

int condicion; // El es un operador de asignación

scanf("%d", &condicion);

if (condicion == 0 ){
	// == Es el operador de comparación
	printf("Condicion vale 0\n");

} 
	if (condicion == 1){
	printf("Condicion vale 1\n");

	} 
	if (condicion != 0 && condicion != 1);
		printf("Condicion no vale ni 1 ni 0");

return 0;
}
